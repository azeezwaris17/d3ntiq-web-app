/**
 * /patient — Patient dashboard home page.
 * Shows the welcome overview: stats, next appointment, notifications, quick actions.
 */
import { PatientDashboardHomePage } from '@/modules/dashboard/patient/presentation/pages/DashboardHomePage';

export default function PatientDashboardRoot() {
  return <PatientDashboardHomePage />;
}
